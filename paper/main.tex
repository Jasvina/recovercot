\documentclass[11pt]{article}
\usepackage[review]{acl}
\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{inconsolata}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{url}

\title{RecoverCoT: Counterfactual Recoverability Distillation for Robust Web Agents}

\author{Anonymous EMNLP Submission}

\begin{document}
\maketitle

\begin{abstract}
Long-horizon web agents frequently fail because intermediate mistakes compound into states from which greedy action generation cannot recover. Existing methods improve next-action prediction, process reward estimation, reflection, branching, or rollback, but they rarely model a more basic question: whether the current state is still recoverable, and if so, how recovery should proceed. We introduce \textit{recoverability} as a structured process-level supervision signal for web agents. Recoverability captures whether an agent can still complete the task from its current trajectory state, whether it should continue, branch, or roll back, and which previous checkpoint should be restored when rollback is needed. We instantiate this idea with \textsc{RecoverCoT}, a counterfactual recoverability distillation framework that labels intermediate states by comparing candidate recovery paths through short rollouts or teacher evaluation. The resulting supervision trains a recoverability-aware controller that governs inference-time recovery decisions before the base agent emits the next web action. We also define recovery-specific metrics, including first-error recovery rate, rollback precision, unnecessary rollback rate, and rollback target accuracy, to isolate recovery quality from overall task success. This draft presents the full formulation, training pipeline, and evaluation protocol needed to test whether explicit recoverability modeling improves success, reduces wasted exploration, and increases robustness under noisy or underspecified instructions.
\end{abstract}

\section{Introduction}
Large language model (LLM) agents are increasingly deployed for long-horizon interactive tasks such as web navigation, form filling, tool use, and information seeking. Unlike single-turn language generation, these tasks require an agent to maintain a dynamic state, interpret delayed feedback, and recover from local mistakes. A single wrong click or premature search refinement can move the agent into a misleading branch of the web environment. If the agent continues greedily from that state, the error may compound across many subsequent actions.

Recent work has therefore moved beyond simple next-action prediction. Web agents have been augmented with reflection, branching, and rollback-style reasoning \citep{hu-etal-2025-webcot}; explicit rollback mechanisms \citep{zhang-etal-2026-webrollback}; reinforcement learning for web interaction \citep{wei-etal-2025-webagentr1}; and process reward models that score intermediate trajectories \citep{choudhury2025agentprm,zhang2026webarbiter}. These directions share an important premise: final task success is too sparse to supervise long-horizon behavior, so agents need process-level feedback.

However, existing process supervision often leaves a key distinction implicit. Not every error should be handled in the same way. Some states are still locally repairable and the agent should continue with a corrective action. Some require branching to test an alternative interpretation. Some are recoverable only if the agent rolls back to a prior checkpoint. Others are effectively irrecoverable under the remaining step budget. A scalar reward or a generic rollback operator does not directly answer these questions.

We argue that web agents need explicit \textit{recoverability} modeling: before choosing the next action, the agent should estimate whether the current state is still recoverable, which recovery mode is appropriate, and where rollback should occur. This perspective reframes recovery as a structured decision problem rather than a side effect of reward scoring or ad hoc reflection.

We propose \textsc{RecoverCoT}, a framework for \textit{Counterfactual Recoverability Distillation}. Given a task, trajectory history, and current observation, \textsc{RecoverCoT} constructs candidate recovery paths such as continuing, branching, rolling back to recent checkpoints, or restarting. Candidate outcomes are estimated with bounded rollouts or a teacher evaluator. These counterfactual comparisons yield structured labels: recoverability level, recovery decision, rollback target, and a concise rationale. A controller is then trained to predict these labels and guide inference-time web-agent execution.

Our contributions are:
\begin{enumerate}[leftmargin=*]
    \item We formalize recoverability as a process-level supervision signal for long-horizon web agents, separating \emph{whether a state is still salvageable} from \emph{which action should be taken next}.
    \item We propose \textsc{RecoverCoT}, a counterfactual data construction and distillation framework that produces structured continue/branch/rollback supervision from intermediate states.
    \item We define a recovery-centric evaluation protocol, including new recovery metrics and explicit robustness settings, so that progress on error correction is measurable rather than hidden inside aggregate success rate.
\end{enumerate}

\section{Related Work}
\paragraph{LLM web agents.}
LLM-based web agents combine natural-language reasoning with interactive browser actions, extending the reasoning-and-acting paradigm of ReAct-style agents \citep{yao2023react}. Benchmarks and systems such as WebVoyager, Mind2Web, and WebArena evaluate whether agents can complete realistic web tasks from observations and action histories \citep{he-etal-2024-webvoyager,deng2023mind2web,zhou2023webarena}. As tasks become longer and environments more dynamic, simple greedy action generation often fails because local errors shift the agent into a wrong branch of the environment.

\paragraph{Reflection, branching, and rollback.}
WebCoT reconstructs chain-of-thought traces for web-agent reflection, branching, and rollback, showing that targeted reasoning skills can improve web navigation \citep{hu-etal-2025-webcot}. WebRollback introduces an explicit rollback mechanism to counter the brittleness of one-way greedy search \citep{zhang-etal-2026-webrollback}. These works demonstrate that recovery operations are useful, but they do not isolate recoverability as a separate state-level prediction problem. Our work is complementary: rather than only adding rollback or teaching rollback rationales, we train the agent to decide when a state is locally repairable, when branching is preferable, and when rollback is warranted.

\paragraph{Process reward models for agents.}
Process reward models provide denser feedback than final success labels. AgentPRM uses Monte Carlo rollouts to estimate process rewards for agent training \citep{choudhury2025agentprm}. WebArbiter develops reasoning-first process reward modeling for web navigation \citep{zhang2026webarbiter}. Such models typically score actions or trajectories, whereas recoverability exposes a structured decision: continue, branch, roll back, or declare the state irrecoverable. This structure makes recovery behavior more interpretable and directly actionable.

\paragraph{Robustness under imperfect guidance.}
Recent benchmarks emphasize that agents must remain robust to imperfect instructions, noisy demonstrations, or misleading guidance \citep{diao-etal-2025-guidebench,fu-etal-2026-beyond}. Recoverability modeling provides a natural mechanism for this setting: when guidance pushes an agent toward a wrong branch, the controller can identify declining recoverability and trigger branch or rollback decisions before the failure becomes irreversible.

\section{Problem Formulation}
We consider a web task specified by an instruction $x$. At step $t$, an agent has trajectory history $h_t = (o_0, a_0, \ldots, o_t)$, where $o_t$ is the current observation and $a_i$ are prior actions. A base policy $\pi_\theta$ proposes actions from $x$ and $h_t$. Standard web-agent training optimizes next-action imitation, final task success, or a scalar process score.

We define a checkpoint set $C_t = \{c_0, \ldots, c_m\}$ drawn from prior trajectory states. Checkpoints may correspond to page transitions, form submissions, search queries, or other semantically meaningful state changes. A recovery decision belongs to
\begin{equation}
\mathcal{D} = \{\textsc{Continue}, \textsc{Branch}, \textsc{Rollback}, \textsc{Restart}\}.
\end{equation}
If the decision is \textsc{Rollback}, the controller also predicts a target checkpoint $c_j \in C_t$.

Recoverability is the probability that a task can still be completed from the current state under a bounded recovery budget $B$:
\begin{equation}
\rho(s_t) = \Pr[\mathrm{success} \mid x, h_t, B].
\end{equation}
In practice, we discretize $\rho$ into three labels: \textsc{Recoverable}, \textsc{WeaklyRecoverable}, and \textsc{Irrecoverable}. The label is paired with the best recovery decision and an optional rollback target.

The key distinction from a scalar reward is structural. A reward model asks whether a trajectory prefix appears promising; a recoverability model asks whether the state can still be repaired \emph{and} which intervention type is best under the remaining budget. This distinction is particularly important in web environments where a local action may be suboptimal even though the overall state remains recoverable, or where an apparently sensible action sequence has already committed the agent to an unrecoverable branch.

\section{Counterfactual Recoverability Distillation}
\subsection{State Sampling}
We sample intermediate states from both successful and failed trajectories. Uniform sampling wastes budget on easy states, so we prioritize states likely to reveal recovery behavior: the first divergence from a successful trajectory, repeated failed attempts, branch changes, form submissions, backtracking events, and late-stage states near success or failure.

\subsection{Checkpoint Mining}
Recoverability decisions are only meaningful if rollback targets correspond to reusable states. We therefore mine checkpoints from trajectory structure rather than every time step. A checkpoint is created when one of the following events occurs: a URL transition, a search query submission, a form commit, a change of authenticated state, or a large DOM transition detected by a state summarizer. This gives a compact candidate set that is easier for both teachers and learned controllers to reason over.

\subsection{Candidate Recovery Construction}
For each sampled state $s_t=(x,h_t,o_t)$, we construct a candidate set $\mathcal{K}_t$:
\begin{align}
\mathcal{K}_t = \{&k_{\mathrm{cont}}, k_{\mathrm{branch}}, k_{\mathrm{rb1}}, \\
&k_{\mathrm{rbk}}, k_{\mathrm{restart}}\}.
\end{align}
The continue candidate follows the current base policy. The branch candidate asks the agent or teacher to generate one or more alternative plausible actions that differ from the current top action. The rollback candidates restore recent or earlier checkpoints. The restart candidate estimates whether abandoning the current trajectory is preferable under budget. In implementation, we only retain a small number of candidates per state, e.g., one continue action, top-$K$ branch actions, and rollback to the most recent $J$ checkpoints, to keep labeling cost bounded.

\subsection{Hybrid Rollout--Teacher Evaluation}
Pure rollout-based labeling is expensive and noisy, while pure teacher labeling may fail to capture environment constraints. We therefore use a hybrid evaluator. Each candidate first receives a bounded rollout of horizon $H$ with the base agent or a stronger teacher agent. Candidates whose outcomes remain ambiguous are then judged by a teacher prompt that compares the candidate state with the task objective, action history, and remaining budget. The teacher returns a success plausibility score, a preferred decision type, and a short natural-language rationale. This hybrid design gives cheaper labels than deep search while preserving access to environment-grounded evidence.

\subsection{Counterfactual Labeling}
Each candidate $k \in \mathcal{K}_t$ is evaluated by bounded rollout, teacher judgment, or both. Let $y_k \in \{0,1\}$ denote task success, $\Delta_k$ the number of additional steps, and $q_k$ an optional quality penalty for unsafe, redundant, or invalid actions. We score candidates as
\begin{equation}
S(s_t,k) = y_k - \lambda \Delta_k - \mu q_k.
\end{equation}
The best candidate $k^*=\arg\max_k S(s_t,k)$ determines the recovery decision. The recoverability class is assigned from the candidate success distribution: states with at least one high-scoring successful continuation are recoverable, states with only costly or fragile successful candidates are weakly recoverable, and states with no successful candidate under budget are irrecoverable.

In practice, we can make the class boundary explicit with thresholds $\tau_{\mathrm{hi}}$ and $\tau_{\mathrm{lo}}$ on the best candidate score:
\begin{equation}
r_t =
\begin{cases}
\textsc{Recoverable}, & S(s_t,k^*) \ge \tau_{\mathrm{hi}} \\
\textsc{WeaklyRecoverable}, & \tau_{\mathrm{lo}} \le S(s_t,k^*) < \tau_{\mathrm{hi}} \\
\textsc{Irrecoverable}, & S(s_t,k^*) < \tau_{\mathrm{lo}}.
\end{cases}
\end{equation}
This definition is flexible: the thresholds can be tuned for aggressive or conservative recovery behavior, and the score can absorb task-specific penalties such as invalid purchase commitments, repeated CAPTCHA loops, or risky side effects.

The final supervision instance is
\begin{equation}
(x,h_t,o_t) \mapsto (r_t,d_t,c_t,z_t),
\end{equation}
where $r_t$ is the recoverability class, $d_t$ the recovery decision, $c_t$ the rollback target when applicable, and $z_t$ a concise rationale.

\subsection{Training Objective}
We train a controller $g_\phi$ to output a structured recovery record. A simple implementation uses instruction tuning over JSON-formatted targets:
\begin{verbatim}
{
  "recoverability": "weakly_recoverable",
  "decision": "rollback",
  "rollback_target": 4,
  "reason": "The current filters depend on an earlier wrong category choice."
}
\end{verbatim}
The loss is a weighted sum of recoverability classification, decision prediction, rollback target prediction, and rationale generation:
\begin{equation}
\mathcal{L}=\mathcal{L}_{r}+\alpha\mathcal{L}_{d}+\beta\mathcal{L}_{c}+\gamma\mathcal{L}_{z}.
\end{equation}
When multiple candidates are available, a preference loss can additionally rank successful low-cost recovery decisions above failed or costly alternatives. In the simplest setup, $g_\phi$ is a single instruction-tuned LLM that predicts structured JSON. A stronger variant separates the controller from the action model: the controller predicts $(r_t,d_t,c_t)$, while the base policy generates the actual web action conditioned on the controller decision and any restored checkpoint.

\section{Recoverability-Aware Inference}
At inference time, the controller is called before the base agent emits the next action. If the state is recoverable and the decision is \textsc{Continue}, the base policy proceeds normally. If the decision is \textsc{Branch}, the system requests an alternative action or short lookahead. If the decision is \textsc{Rollback}, the browser state is restored to the predicted checkpoint and the base policy resumes from there. If the state is irrecoverable, the agent restarts or terminates depending on task budget.

To reduce overhead, the controller need not run at every step. It can be triggered on high-risk events such as repeated invalid actions, task stagnation, large page transitions, or large drops in a lightweight value heuristic. This trigger policy turns recoverability into a cheap online guardrail rather than a full second policy running continuously.

This design separates recovery control from action generation. The base agent remains responsible for low-level web actions, while the controller decides whether the current trajectory is worth continuing. The separation also makes ablations straightforward: we can swap the controller while keeping the same underlying action model.

\section{Experimental Protocol}
\subsection{Benchmarks}
We evaluate on WebVoyager, Mind2Web-Live, and WebArena-Lite \citep{he-etal-2024-webvoyager,deng2023mind2web,zhou2023webarena}. These benchmarks provide long-horizon web tasks with realistic interaction histories, making them suitable for studying recovery after intermediate errors. In this draft, we separate two settings:
\begin{enumerate}[leftmargin=*]
    \item \textbf{Offline counterfactual evaluation}, where intermediate states are sampled from recorded trajectories and labeled with the proposed counterfactual procedure.
    \item \textbf{Online agent evaluation}, where a live or replayable web agent uses the controller during task execution.
\end{enumerate}
The offline setting isolates recovery decisions from environment instability, while the online setting measures end-to-end usefulness.

\subsection{Training Data Construction}
Training examples are built from mixed successful and failed trajectories. We use state sampling heuristics from Section 4 to enrich hard states, then create train/dev/test splits at the task level to avoid state leakage from the same trajectory. When benchmark websites overlap heavily, we additionally recommend site-level or template-level separation to prevent the controller from simply memorizing domain-specific checkpoints.

\subsection{Baselines}
We compare against four baseline families:
\begin{enumerate}[leftmargin=*]
    \item a greedy web agent without explicit recovery;
    \item a WebCoT-style reasoning baseline with reflection and rollback rationales;
    \item a WebRollback-style explicit rollback baseline;
    \item a scalar process reward or reranking baseline.
\end{enumerate}
This comparison separates recoverability-aware control from general reasoning, rollback availability, and scalar reward modeling. Where exact implementations are unavailable, we implement faithful approximations with the same backbone model and action space.

\subsection{Metrics}
Besides task success rate, average steps, and token cost, we report recovery-specific metrics.
\paragraph{First-Error Recovery Rate.}
The fraction of trajectories that eventually succeed after the first detected divergence from a successful or teacher-preferred trajectory.
\paragraph{Rollback Precision.}
Among all triggered rollbacks, the fraction for which rollback yields a better counterfactual outcome than continuing.
\paragraph{Unnecessary Rollback Rate.}
The fraction of rollbacks triggered in states that were locally repairable by continuing or branching.
\paragraph{Rollback Target Accuracy.}
The fraction of rollback decisions whose predicted checkpoint matches the best counterfactual checkpoint.

\subsection{Robustness Settings}
To test whether recoverability helps under imperfect guidance, we evaluate with three perturbation settings inspired by recent robustness benchmarks \citep{diao-etal-2025-guidebench,fu-etal-2026-beyond}: underspecified instructions, partially misleading hints, and noisy demonstrations or retrieval context. The controller should be especially useful in these settings because guidance errors often create wrong-branch trajectories before the task becomes unsalvageable.

\subsection{Ablations}
We ablate binary recoverability versus structured recoverability, decision prediction without rollback targets, training without rationales, final-outcome weak labels instead of counterfactual labels, and controller-only inference without branch generation. These ablations test whether the gains come from recoverability structure rather than from additional supervision volume alone.

\subsection{Implementation Details}
The intended minimal implementation uses a 7B--8B instruction model with LoRA or QLoRA for controller tuning. Candidate labeling can be generated with a stronger API teacher or with a stronger open model, depending on cost constraints. We recommend reporting the controller call rate, added latency, and label-generation budget because recoverability must be useful at practical inference cost, not only at unconstrained compute.

\section{Analysis Plan}
We analyze errors by category: locally repairable mistakes, wrong-branch mistakes, irreversible form submissions, instruction ambiguity, and environment instability. We also measure calibration between predicted recoverability and empirical rollout success. Case studies contrast over-persistence, where baselines continue in low-recoverability states, with over-rollback, where baselines abandon states that remain locally repairable.

An especially important analysis is the gap between binary success and recoverability quality. A controller may improve rollback precision and reduce unnecessary rollback even when overall task success changes only modestly. We therefore report both end-to-end and recovery-specific outcomes so that the paper does not overclaim from one metric.

\section{Reproducibility Checklist}
\begin{enumerate}[leftmargin=*]
    \item Release the prompts and schema used for teacher-based recoverability labeling.
    \item Release the checkpoint mining rules and any browser-state summarization templates.
    \item Log all candidate recoverability decisions for at least a subset of evaluation tasks.
    \item Report controller overhead, API cost for label generation, and action-model backbone details.
    \item Include a human spot-check of recoverability labels to estimate label noise.
\end{enumerate}

\section{Limitations}
Recoverability labels depend on the quality of bounded rollouts or teacher evaluation. If the teacher underestimates a recovery path, the controller may learn conservative behavior. Live web benchmarks also introduce environment drift. To reduce these risks, the evaluation should include offline counterfactual diagnostics, human spot checks of labels, and small-scale live validation.

The current draft also intentionally avoids fabricating empirical claims. The method, metrics, and experimental protocol are fully specified, but numerical results still need to be populated from real runs before submission.

\section{Conclusion}
We proposed \textsc{RecoverCoT}, a framework for teaching web agents to reason about whether their current state is recoverable and which recovery mode should be used. By turning recovery into a structured process-level prediction problem, counterfactual recoverability distillation offers a lightweight alternative to full reinforcement learning while directly addressing a central failure mode of long-horizon agents.

\appendix

\section{Recoverability Record Schema}
Table~\ref{tab:label-schema} summarizes the fields used by the proposed recoverability labeling pipeline. The same schema is mirrored in the repository so the paper draft and experiment implementation stay aligned.

\begin{table*}[t]
\centering
\small
\begin{tabular}{lll}
\toprule
Field & Type & Description \\
\midrule
task\_id & string & Unique identifier for the task instance. \\
state\_id & string & Unique identifier for the sampled intermediate state. \\
instruction & string & Natural-language task instruction. \\
history & array & Prior observations/actions up to the current state. \\
checkpoint\_ids & array & Candidate rollback checkpoints available at this state. \\
recoverability & string & One of recoverable, weakly\_recoverable, irrecoverable. \\
decision & string & One of continue, branch, rollback, restart. \\
rollback\_target & string/null & Selected checkpoint id when decision is rollback. \\
candidate\_scores & object & Optional score dictionary for continue/branch/rollback candidates. \\
teacher\_rationale & string & Short explanation supporting the selected label. \\
\bottomrule
\end{tabular}
\caption{Structured fields for a recoverability supervision record.}
\label{tab:label-schema}
\end{table*}

\section{Teacher Prompt Template}
The teacher prompt should expose the task instruction, compressed state summary, recent trajectory history, candidate recovery options, and the remaining budget. The teacher output must be machine-readable so that labels can be validated automatically before controller training. A repository version of the prompt template is provided for direct experimentation.

\section{Result Table Templates}
Tables~\ref{tab:main-results-template} and~\ref{tab:ablation-template} are draft-time templates for the final paper. Entries are left blank intentionally until real experiments are completed.

\begin{table*}[t]
\centering
\small
\begin{tabular}{lcccccc}
\toprule
Method & Success $\uparrow$ & First-Error Recovery $\uparrow$ & Rollback Precision $\uparrow$ & Unnecessary Rollback $\downarrow$ & Steps $\downarrow$ & Tokens $\downarrow$ \\
\midrule
Greedy agent & -- & -- & -- & -- & -- & -- \\
WebCoT-style baseline & -- & -- & -- & -- & -- & -- \\
WebRollback-style baseline & -- & -- & -- & -- & -- & -- \\
Scalar PRM / reranker & -- & -- & -- & -- & -- & -- \\
RecoverCoT & -- & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{Main online evaluation template.}
\label{tab:main-results-template}
\end{table*}

\begin{table*}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Variant & Decision Acc. $\uparrow$ & Rollback Target Acc. $\uparrow$ & Rollback Precision $\uparrow$ & Success $\uparrow$ \\
\midrule
Binary recoverability only & -- & -- & -- & -- \\
No rollback target head & -- & -- & -- & -- \\
No rationale supervision & -- & -- & -- & -- \\
Weak labels only & -- & -- & -- & -- \\
Full RecoverCoT & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{Ablation template for offline and online recovery quality.}
\label{tab:ablation-template}
\end{table*}

\bibliography{references}
\bibliographystyle{acl_natbib}
\end{document}
